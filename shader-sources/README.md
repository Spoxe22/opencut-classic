# GLSL reference sources

These files are the immutable GLSL references imported from Slideshow AI. OpenCut compiles the corresponding WGSL ports with the application; it does not execute arbitrary GLSL at runtime.

Attribution follows the headers in each source:

- MIT: DreamyZoom (Zeh Fernando), DreamyZoomFilmBurn variants, GlitchDisplace (Matt DesLauriers), MotionMachineFlash (Motion Machine), Overexposure (Ben Zhang), tangentMotionBlur (chenkai), plus the Slideshow AI film-roll, river, Super8Frame and FilmRollSix additions.
- BSD-2-Clause: StereoViewerVertical, inspired by StereoViewer by Ted Schundler.

The project-level MIT license applies to original Slideshow AI ports and additions. Upstream author and license headers are intentionally retained in each GLSL file.
